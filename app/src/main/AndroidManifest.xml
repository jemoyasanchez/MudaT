<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itla.mudat">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
            <activity
            android:name=".SplashActivityIni"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_splash_ini"
            android:theme="@style/FullscreenTheme"></activity>
        -->
        <activity
            android:name=".SplashActivityInic"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.RegistroCategoria"
            android:configChanges="" />
        <activity
            android:name=".MenuPrincipal"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_menu_principal"
            android:screenOrientation="sensor"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.RegistroAnuncio" />
        <activity android:name=".View.Publicaciones" />
        <activity android:name=".View.MyPublicaciones" />
        <!--
               <activity
              android:name=".ActividadIni"
              android:configChanges="orientation|keyboardHidden|screenSize"
              android:label="@string/title_activity_actividad_iniciar"
              android:theme="@style/FullscreenTheme"></activity>
        -->

        <activity android:name=".IniciarSesionInic" />
        <activity android:name=".ContenidoIniciar" />
        <activity android:name=".PerfilDeUsuario"></activity>
    </application>

</manifest>